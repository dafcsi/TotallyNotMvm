import { KiemeltAllasPipe } from './kiemelt-allas.pipe';

describe('KiemeltAllasPipe', () => {
  it('create an instance', () => {
    const pipe = new KiemeltAllasPipe();
    expect(pipe).toBeTruthy();
  });
});
