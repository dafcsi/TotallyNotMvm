import { DisableIfAdminDirective } from './disable-if-admin.directive';

describe('DisableIfAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableIfAdminDirective();
    expect(directive).toBeTruthy();
  });
});
